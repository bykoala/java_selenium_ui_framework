/*
 * package actionUtils;
 * 
 * import org.testng.log4testng.Logger;
 * 
 * 
 * 提供页面元素定位的公共方法，如选中下拉框某个值； 获取列表中的某一行某一列的值； 获取某个元素的WebElement的值等等。
 * 
 * public class UIUtil {
 * 
 * // private static Logger logger = Logger.getLogger("runLog"); public static
 * String DROP_DOWN_BOX =
 * "//div[contains(@class,'tt-dropdown-con) and contains(@class,'ng-iso]";
 * public static String LEFTMENU = "//div[contains(@class,'ti-leftmenu";
 * 
 * public static void movePage(int num) {
 * logger.info("============[movePage start]=============="); //
 * ((JavascriptExecutor)BasicAction.getDriver()).executeScript(
 * "window.scrollBy(0," + num + ")" ),new Object([0]); } }
 */